{
  "name": "rubiks-cube-solver",
  "version": "1.0.0",
  "description": "Rubik's Cube Solver - React frontend + Node.js backend",
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend-node install",
    "test": "cd backend-node && npm install && cd .. && node test-script.js",
    "start": "node backend-node/server.js"
  }
}
